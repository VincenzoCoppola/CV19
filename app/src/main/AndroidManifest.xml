<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pk.cv19">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/travel_ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/travel_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecensioniEStrutture.FiltraRecensioni"></activity>
        <activity android:name=".MappaESplash.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MappaESplash.Mappa"
            android:label="@string/title_activity_map" />
        <activity android:name=".Menù.Menu_Ospite" />
        <activity android:name=".RecensioniEStrutture.Recensioni_Personali" />
        <activity android:name=".Menù.Menu_Personale" />
        <activity android:name=".RicercheEFiltri.Ricerca_Avanzata" />
        <activity android:name=".RicercheEFiltri.Effettua_Ricerca_Avanzata" />
        <activity android:name=".RicercheEFiltri.Ricerca" />
        <activity android:name=".RecensioniEStrutture.FaiRecensione" />
        <activity android:name=".RecensioniEStrutture.Mostra_Struttura_Dettaglio" />
        <activity android:name=".RegistrazioneELogin.RECUPERA" />
        <activity android:name=".RegistrazioneELogin.Registrazione" />
        <activity android:name=".RegistrazioneELogin.LOGIN"></activity>
        <activity
            android:name=".RecensioniEStrutture.MainActivity"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>